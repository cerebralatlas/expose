{
  "name": "expose",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "bin": {
    "expose": "./src/cli.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "build": "bun build ./src/cli.ts --compile --outfile expose.exe"
  }
}
